<?xml version="1.0" encoding="utf-8"?>
<layout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
                name="plannedSet"
                type="vzijden.workout.domain.model.PlannedSet"/>
    </data>

    <vzijden.workout.view.edit.exercise.SetView
            android:layout_gravity="center"
            android:id="@+id/schedule_item_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">


        <TextView
                android:text="@string/reps"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/set_item_view_label"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="Reps:"
                android:textStyle="bold"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintVertical_bias="0.0"/>

        <TextView
                android:layout_marginStart="8dp"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:id="@+id/set_item_view_amount"
                app:layout_constraintStart_toEndOf="@id/set_item_view_label"
                android:textAppearance="@style/TextAppearance.AppCompat.Caption"
                tools:text="8"
                android:text="@{String.valueOf(plannedSet.reps)}"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"/>

        <SeekBar
                android:max="@integer/max_set_reps"
                android:progress="@{plannedSet.reps}"
                android:layout_height="wrap_content"
                android:id="@+id/set_item_view_seek_bar"
                android:layout_width="0dp"
                android:layout_marginStart="8dp"
                app:layout_constraintStart_toEndOf="@+id/set_item_view_amount"
                android:layout_marginEnd="8dp"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintHorizontal_bias="0.5"/>
    </vzijden.workout.view.edit.exercise.SetView>
</layout>
