<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
                name="viewModel"
                type="vzijden.workout.view.schedule.workout.exercise.ExerciseItemViewModel"/>
        <variable
                name="editWorkoutPresenter"
                type="vzijden.workout.view.schedule.workout.EditWorkoutPresenter"/>
    </data>

    <vzijden.workout.view.schedule.workout.exercise.ExerciseItemView
            android:gravity="center"
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clickable="true"
            android:onClick="@{() -> editWorkoutPresenter.onExerciseClicked(obj)}">

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textStyle="bold"
                android:textSize="28sp"
                android:id="@+id/schedule_item_view_workout_name_text_field"
                android:text="@={viewModel.index}"
                tools:text="1"/>

        <com.google.android.material.card.MaterialCardView
                android:id="@+id/cardview"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="4dp"
                android:layout_marginTop="4dp"
                android:layout_marginBottom="4dp"
                app:cardCornerRadius="0dp">

            <androidx.constraintlayout.widget.ConstraintLayout
                    android:layout_width="match_parent"
                    android:padding="8dp"
                    android:layout_height="match_parent">

                <TextView
                        tools:text="Bench Press"
                        android:textColor="@color/text_dark"
                        android:text="@={viewModel.exerciseName}"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:id="@+id/workout_item_view_name"/>

                <TextView
                        app:layout_constraintTop_toBottomOf="@+id/workout_item_view_name"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textStyle="bold"
                        android:id="@+id/workout_item_view_muscle_group"
                        android:text="@={viewModel.muscleGroup}"
                        tools:text="Chest"/>

                <androidx.recyclerview.widget.RecyclerView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:maxHeight="100dp"
                        app:layout_constraintTop_toBottomOf="@id/workout_item_view_muscle_group"
                        android:id="@+id/workout_item_view_sets_list"
                        app:data="@{viewModel.sets}"
                        app:changedPositions="@{viewModel.changedPositions}">
                </androidx.recyclerview.widget.RecyclerView>
            </androidx.constraintlayout.widget.ConstraintLayout>
        </com.google.android.material.card.MaterialCardView>
    </vzijden.workout.view.schedule.workout.exercise.ExerciseItemView>


</layout>
